class: GeneratePresignedUploadUrlRequestDto
fields:
  fileName: String
  contentType: String
  fileSize: int
  bucketType: String # "public" or "private"
  prefix: String? # 파일 경로 prefix (예: "products", "profiles", "chats")

